ext {
  VERSION_CODE = 19
  VERSION_NAME_SUFFIX = System.getenv("BUILD_NAME_SUFFIX") != null ? System.getenv("BUILD_NAME_SUFFIX") : ""
  VERSION_NAME = "0.5.0.0" + VERSION_NAME_SUFFIX
  VERSION = VERSION_NAME
  GROUP = "com.asfoundation"
}