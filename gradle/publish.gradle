ext {
  VERSION_CODE = 106
  VERSION_NAME_SUFFIX =
      System.getenv("BUILD_NAME_SUFFIX") != null ? System.getenv("BUILD_NAME_SUFFIX") : ""
  VERSION_NAME = "0.6.2.0" + VERSION_NAME_SUFFIX
  VERSION = VERSION_NAME
  GROUP = "com.blockchainds"
}